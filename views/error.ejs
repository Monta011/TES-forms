<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen flex flex-col">
  <%- include('partials/navbar') %>
  
  <main class="flex-grow container mx-auto px-4 py-8">
    <div class="max-w-2xl mx-auto text-center py-12">
  <svg class="mx-auto h-24 w-24 text-red-400 mb-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
  </svg>
  <h1 class="text-4xl font-bold text-gray-800 mb-4"><%= title || 'Error' %></h1>
  <p class="text-lg text-gray-600 mb-8"><%= message || 'Something went wrong' %></p>
  <% if (error && error.stack && process.env.NODE_ENV === 'development') { %>
    <div class="bg-red-50 border border-red-200 rounded-lg p-4 mt-6 text-left">
      <pre class="text-sm text-red-800 overflow-auto"><%= error.stack %></pre>
    </div>
  <% } %>
  <a href="/" class="inline-block bg-blue-600 hover:bg-blue-700 text-white font-semibold py-3 px-8 rounded-lg transition mt-6">
    Go Back Home
  </a>
</div>
  </main>

  <%- include('partials/footer') %>
</body>
</html>
